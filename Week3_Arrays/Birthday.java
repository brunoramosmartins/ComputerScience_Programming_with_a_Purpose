public class Birthday {
    public static void main(String[] args) {

        int n = Integer.parseInt(args[0]);
        int trials = Integer.parseInt(args[1]);
        int[] qtd = new Int[n + 1]


        for (int i = 0; i < trials; i++) {
            int bday = Math.random() * n + 1;

            

            
        }
    }
}